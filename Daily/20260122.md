# January 22, 2026

- Another look at simple linear regression using linear algebra and geometry.
- remember the norm and the dot product
- Turn the data "sidewise"
- add a column of ones for the bias
- minimizing the MSE is the same as finding the "closest point"

- Higher dimensions
- Data MatrixS
- Features (continuous, categorical)
- Samples
- Feature space
- Target variable

Matrix multiplication:
MX computes a linear combination of the columns of M
AB via rows \* columns

C=AB

c*{rs} = sum a*{ri}b\_{is}

- multivariate optimization problem
- again a convex loss function

Add a column of 1's to X
Y = XM
M are the "weights"

Calculus and the gradient
minimize by finding gradient of Loss and setting equal to zero
Matrix calculus

dE/dM*t = sum_j=1)^{N} X*{jt} (Y*{j}- sum X*{js}M_s) = dot product of column t of X with Y-XM
stack em up yields
X^T(Y-XM)

Correct formula is -2(X^TY-X^TXM)

More linear algebra

- span
- dot product
- linear combination
- orthogonality

X is n x m
Y is n x k

X^T Y is m x k -- entries are dot products of columns of X and Y

can read this as (X^T)\cdot (Y-XM)=0

D = X^T X
M = (D^{-1})X^TY

D must be invertible for this to work

This happens if the columns (features) are independent.
Dv = 0 means the vector Xv is perpendicular to every column of X.
or v^TX^TXv=0 which means the vector Xv=0 so the columns are dependent.

geometric interpretation

centering the coordinates simplifies the problem.
